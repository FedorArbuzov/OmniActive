# Database
DATABASE_URL=postgresql://user:password@localhost:5432/omniactive

# JWT
SECRET_KEY=your-secret-key-change-in-production-use-random-string
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# AWS
AWS_REGION=us-east-1

# CORS — URL, с которого открыт фронт (обязательно для выложенного фронта)
# Один origin строкой или JSON-массив. Без этого запросы с фронта будут блокироваться браузером.
# Пример: CORS_ORIGINS=https://my-app.vercel.app
# Пример: CORS_ORIGINS=["https://my-app.vercel.app","https://my-app.com","http://localhost:8080"]
CORS_ORIGINS=
